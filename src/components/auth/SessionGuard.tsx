'use client';

/**
 * @fileOverview Session Guard (Neutralized).
 * ENFORCEMENT DISABLED: Multi-device login is now allowed as per user request.
 * Users can now use the platform on phone and desktop simultaneously.
 */
export default function SessionGuard() {
  // Logic removed to allow multiple active sessions per account.
  return null;
}
