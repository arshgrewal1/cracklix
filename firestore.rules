
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global permissive rule for prototype stability and rapid iteration.
    // This allows all read, write, and list operations across all collections.
    match /{document=**} {
      allow read, list, write: if true;
    }
  }
}
