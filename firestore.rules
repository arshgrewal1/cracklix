
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper to check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }

    // Profiles: Users can only read/write their own profile
    match /users/{userId} {
      allow read, write: if isAuthenticated() && request.auth.uid == userId;
    }

    // Public content: Anyone can read (get/list), only authenticated users (admins) can write
    match /boards/{id} { allow read: if true; allow write: if isAuthenticated(); }
    match /exams/{id} { allow read: if true; allow write: if isAuthenticated(); }
    match /subjects/{id} { allow read: if true; allow write: if isAuthenticated(); }
    match /questions/{id} { allow read: if true; allow write: if isAuthenticated(); }
    match /mocks/{id} { allow read: if true; allow write: if isAuthenticated(); }
    match /current_affairs/{id} { allow read: if true; allow write: if isAuthenticated(); }
    match /notifications/{id} { allow read: if true; allow write: if isAuthenticated(); }
    match /settings/{id} { allow read: if true; allow write: if isAuthenticated(); }

    // Results: Users can only read/write their own results
    match /results/{id} {
      allow create: if isAuthenticated();
      allow read: if isAuthenticated() && (resource == null || resource.data.userId == request.auth.uid);
    }
    
    // Deployment trigger: Ensure rules are updated for current_affairs and notifications list operations
    // Triggered at: 2024-05-20T10:00:00Z
  }
}
