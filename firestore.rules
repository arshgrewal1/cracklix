
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // DEV MODE: Full open access to unblock development and data seeding.
    // This allows all collections (mocks, sessions, analysis, etc.) to be listed and read.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
