
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "User account information and role-based status.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "gender": { "type": "string", "enum": ["Male", "Female", "Other"] },
        "role": { "type": "string", "enum": ["SUPER_ADMIN", "ADMIN", "CONTENT_MANAGER", "STUDENT"] },
        "state": { "type": "string", "enum": ["Punjab"] },
        "targetExam": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "description": "The ID of the active pass or 'Free'" },
        "passExpiryDate": { "type": "string", "format": "date-time" },
        "referralCount": { "type": "number" },
        "subscriptions": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["id", "name", "email", "role"]
    },
    "Pass": {
      "title": "Institutional Pass",
      "description": "A dynamic access tier created by admin.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "price": { "type": "number" },
        "durationDays": { "type": "number" },
        "features": { "type": "array", "items": { "type": "string" } },
        "active": { "type": "boolean" },
        "displayOrder": { "type": "number" },
        "recommended": { "type": "boolean" },
        "adFree": { "type": "boolean" },
        "type": { "type": "string", "enum": ["FREE", "PREMIUM"] },
        "description": { "type": "string" }
      }
    },
    "FreeContent": {
      "title": "Free Content",
      "description": "Unified repository for free study material.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "type": { "type": "string", "enum": ["mock", "pdf", "current", "pyq", "note"] },
        "link": { "type": "string" },
        "fileUrl": { "type": "string" },
        "category": { "type": "string", "default": "free" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "title", "type"]
    }
  },
  "auth": {
    "providers": ["google", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/backend/entities/UserProfile" },
      "description": "Student and Admin profiles."
    },
    "/passes/{passId}": {
      "schema": { "$ref": "#/backend/entities/Pass" },
      "description": "Dynamic pricing tiers."
    },
    "/free_content/{contentId}": {
      "schema": { "$ref": "#/backend/entities/FreeContent" },
      "description": "Unified free study hub registry."
    }
  }
}
