include ':app'

// Resolve repository conflicts by centralizing declarations
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

apply from: 'capacitor.settings.gradle'
